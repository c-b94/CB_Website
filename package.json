{
  "name": "cb-website",
  "version": "1.0.0",
  "description": "personal site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "author": "cedric boudreaux",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.3",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "nodemon": "^3.0.1"
  }
}
